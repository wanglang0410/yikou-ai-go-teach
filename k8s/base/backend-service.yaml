apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: yikou-ai
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8123
      targetPort: 8123
